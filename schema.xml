<dataset>
    <table name="test_name" timestamp="True" id="True">
        <column name="name">varchar(256)</column>
        <column name="product">VARCHAR(256)</column>
    </table>

    <table name="Data_point" link="test_name" id="true">
        <column name="Time_elapsed">DOUBLE</column>
    </table>

    <table name="number" link="Data_point">
        <column name="number">INT</column>
        <column name="type">VARCHAR(256)</column>
    </table>

    <table name="string" link="Data_point">
        <column name="string">VARCHAR(4096)</column>
    </table>
</dataset>

<!--
<Table name datetime=true id=true link=previous_table>
    <Data>

-->
<!-- 
Test
    ID (inherently added, shouldn't need to mention)
    datetime (inherently added, shouldn't need to mention)
    name
    product

Data_point
    ID (inherently added)
    Test_ID (inherently added)
    Time_elapsed

Number
    ID (inherently added)
    Data_point_id (inherently added)
    Number
    Type - Volts, Amps, Degrees etc.

String
    ID (inherently added)
    Data_point_id (inherently added)
    String 


-->
